Description
===============
A django project that uses the method of linear regression to predict salaries based on employee's seniority. Data is loaded from the SQLite database, with the project enclosed is a script to transfer a data from a csv file to the database (csv_to_sqlite.py) and a csv with sample data.